#include<stdio.h>
/**
  *main -> prints alphabet in lowercase, then in uppercase
  *
  *Return: 0 (sucsses)
  */
int main(void)
{
	for (char cl = 'a'; cl <= z; cl++)
		putchar(cl);
	for (char cu = 'A'; cl <= Z; cu++)
		putchar(cu);
	putchar('\n')
	return (0)
}
